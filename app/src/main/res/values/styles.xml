<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Widget.AppTheme.LikeButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="iconTint">@color/like_button_tint</item>
        <item name="android:minWidth">@dimen/iconSize</item>
        <item name="android:minHeight">@dimen/iconSize</item>
    </style>

    <style name="Widget.AppTheme.ShareButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="iconTint">@color/share_button_tint</item>
        <item name="android:minWidth">@dimen/iconSize</item>
        <item name="android:minHeight">@dimen/iconSize</item>
    </style>

    <style name="Widget.AppTheme.ViewsButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="iconTint">@color/default_button_tint</item>
        <item name="android:minWidth">@dimen/iconSize</item>
        <item name="android:minHeight">@dimen/iconSize</item>
    </style>

    <style name="Widget.AppTheme.OptionsMenuButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="iconTint">@color/options_menu_button_tint</item>
        <item name="android:minWidth">@dimen/iconSize</item>
        <item name="android:minHeight">@dimen/iconSize</item>
    </style>
</resources>